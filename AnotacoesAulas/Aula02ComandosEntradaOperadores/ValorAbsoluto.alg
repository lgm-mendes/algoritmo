algoritmo "ValorAbsoluto"
var
   A: Inteiro
inicio
      A <- Abs(-50)
      Escreva(A)
fimalgoritmo

// Funções Aritiméticas
// Abs(Valor absoluto), Exp(Exponenciação), Int(Valor Inteiro),
// RaizQ(Raiz Quadrada), Pi(Retorna Pi), Sen(Seno (rad)),
// Cos(Cosseno(rad)), Tan(Tangente(rad), GraupRad(Graus para Rad).
